import { DetectEnterSideDirective } from './detect-enter-side.directive';

describe('DetectEnterSideDirective', () => {
  it('should create an instance', () => {
    const directive = new DetectEnterSideDirective();
    expect(directive).toBeTruthy();
  });
});
