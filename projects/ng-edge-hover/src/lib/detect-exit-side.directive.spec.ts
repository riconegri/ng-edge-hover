import { DetectExitSideDirective } from './detect-exit-side.directive';

describe('DetectExitSideDirective', () => {
  it('should create an instance', () => {
    const directive = new DetectExitSideDirective();
    expect(directive).toBeTruthy();
  });
});
