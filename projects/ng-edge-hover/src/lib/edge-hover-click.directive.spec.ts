import { EdgeHoverClickDirective } from './edge-hover-click.directive';

describe('EdgeHoverClickDirective', () => {
  it('should create an instance', () => {
    const directive = new EdgeHoverClickDirective();
    expect(directive).toBeTruthy();
  });
});
