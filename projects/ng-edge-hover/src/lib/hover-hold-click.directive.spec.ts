import { HoverHoldClickDirective } from './hover-hold-click.directive';

describe('HoverHoldClickDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverHoldClickDirective();
    expect(directive).toBeTruthy();
  });
});
